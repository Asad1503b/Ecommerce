// one of the most important topic make you to not ittrate thr loop but to own self
import seriesData from './seriesData'
const newData = seriesData.map((ItteratedData) => {
    return `${ItteratedData.sname}`
    
})
console.log(newData)